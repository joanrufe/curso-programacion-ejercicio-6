/**
 * Adjuntar manejadores de eventos (eventHandlers) para que al
 * hacer click en el checkbox, se complete la Tarea; y al hacer
 * click en el boton borrar, se elimine la tarea del listado
 */
export default function adjuntarManejadores() {
  const appContainer = document.querySelector("#todo-app");
  // Debe subscribirse al evento "input" de empleadosInput
}
